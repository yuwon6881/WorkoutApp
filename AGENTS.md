# WorkoutApp guidance

Applies only to this independent repository; app-specific rules override the parent's FinancialApp rules. `CLAUDE.md` is canonical; keep `AGENTS.md` byte-identical. After editing, run `node scripts/sync-docs.mjs`, then verify with `node scripts/sync-docs.mjs --check`.

## Keep this guidance concise

- Update the implemented-feature index in the same change as a feature addition, removal, or material behavior change. Update an existing bullet before adding one; group capabilities and link to detail instead of keeping a changelog here.
- List implemented behavior only; plans are not shipped features. Keep this file near 100 lines or fewer. Product details belong in `README.md`, deployment instructions in `deploy/README.md`, and improvement work in `ENHANCEMENT_PLAN.md`.

## Implemented feature index

- FitnessAccount central sign-in, profile/preferences, and durable Workout/Nutrition connection consent with scoped, short-lived shared-data access.
- Global and account-owned exercise libraries with primary/secondary muscle metadata and category classifications (Free Weights, Machine, Body Weight), primary plus single secondary muscle display with abstracted overflow tags, top-right standardized category pill placement, matching-height search and minor filter controls, category-grouped library views with current-category prioritization, swap-picker pinned current exercise with non-clickable Current badges, standardized title truncation, relevancy boundaries separating similar exercises with full-catalog expansion and global search, ranked catalog alternatives, progress insights/history clearing, manual templates/programs with targeted muscle tags, a shared editable block/week/day builder held in the browser until the program is created for scratch-built and imported programs (contiguous weeks, up to 7 days per week, 104 weeks, and 400 total days), partner-selection superset pairing, exercise-level rest timers, borderless optical day-row controls, a trailing add-day action row, anchored non-clipping chip menus, and responsive set prescription editing, standalone and program swap restore to default baselines with catalog-backed substitutions, multi-phase ordered progression without calendar delays, and AI PDF extraction with source-marked day titles (rotated, vocabulary, and stacked margin titles, with superseded spine tabs kept out of table rows) and authoritative source-label reconciliation, corrected rotated-text geometry, header-aligned multi-layout reconstruction including indexed 2–4-set RIR and dual-RPE tracking, multi-line wrapped table row reconstruction under header bands (padded and top-aligned rows, hyphen-wrapped names, day titles standing in for the name column, footer notes kept out of the last row) preventing movement fusion, table evidence that never mistakes a data row for a header, source-validated outline and repeated-block/phase reconciliation, sequential multi-phase schedule cycle normalization into absolute program weeks preventing week overload, visible non-blocking import notes, one-to-one source-row recovery of fused names and prescriptions, background progress and cancellation that keeps reporting on a floating progress pill once the import screen is left and is refused outright while a workout is open, technique-aware catalog matching, superset detection and pairing, dual working sets with RIR-to-RPE conversion, explicit and compound rep prescriptions, preserved %1RM source loads, compact effort-icon cell and 0–4/5+ RIR modal selection, averaged rest ranges and ~ rest prefixes, expanded idempotent catalog seeds and aliases for Push/Pull/Legs movements, standardized responsive set editing, named alternative-program selection with advertised-but-absent version recovery, source-grounded exercise names and occurrence counts that report a movement the section pages never print, video demonstration links read from the document annotation layer and carried into the program and active workout, 7-day week reconciliation with trailing rest trimming, lettered week versions (such as 10A/10B) kept as separate weeks, overflow detection, and cross-phase week-gap review, exercise removal with superset repair, swipe-to-delete on compact layouts, chip-level block and week actions with drag-and-drop or keyboard day ordering, and uplifted searchable editable review with add/delete week management, revision-checked queued saves for imports, grouped slot mapping that links one written movement once per block across days, weeks, positions, and casing, and default draft/exercise restore with local busy states that blocks program creation until mappings and blocking issues are resolved, plus sequential program runs that show one week at a time, allow any pending workout and rest day to be passed, lock passed days, support guarded current-week resets, and return to standby after the final week.
- Active workout drafts with carousel exercise navigation, focused set logging, progression badges, quick action pills, and session standby readiness states with quick-action paths when no program or workout is active; weight/rep/RPE logging, catalog-backed exercise substitutions restricted to before set completion with default restoration, completed-session history, and a deadline-based rest timer configured per exercise prescription (skipping rest before superset partners or after the final exercise set) that starts after a completed set with a soothing two-note chime. The active workout is recoverable in account-scoped IndexedDB with durable set patches and ordered pause/resume/finish operations; elapsed time and rest deadlines recover from timestamps, and users can opt into local sound, vibration where supported, and visible-screen wake lock. When configured, private per-device rest alerts use durable, generation-scoped Cloud Tasks and short-lived FCM delivery with state rechecks before sending; a visible shell suppresses the push only after confirming ownership of the same account/session/rest generation. Sign-out best-effort cancels the authenticated device schedules and retires its local push token; already-expired authentication can leave cleanup to the short alert expiry and dispatch rechecks. Server confirmation remains authoritative; exercise-list changes and discard still require a connection.
- Rep/load progression, effort-based suggestions, lighter-week recommendations, bodyweight/resistance-mode handling, and Nutrition-informed adaptive tiers.
- Redesigned responsive progress stats showcase with semantic icon badges, highlight accents, empty-state dash indicators, exercise-aware aggregates, actual completed/in-progress workout activity calendar states, estimated 1RM personal records integrated into workout history (collapsed session PR count badges, expanded exercise e1RM bests, and set markers) replacing standalone personal bests, chart-first exercise details with compact secondary records, and an unmounting-safe Muscles coverage map (renamed with BicepsFlexed icon) for the last week, month, or three months, with a unified Command Center Overview combining current-day Quick Start, revamped weekly Training Activity Calendar with day cards and session review modals, progress metrics, and expandable workout history. Completed working sets credit the primary muscle fully and secondary muscles at half weight; one accent shade deepens with credited sets relative to the window’s busiest muscle, hover, focus, and tap all read the same fixed detail slot, and unresolved sets are reported as unattributed. Scoped Nutrition context and shared training summaries; Nutrition owns weight/goals and Workout owns training history/progression.

## UI standardization

- Search `web/src/components/ui/`, nearby feature components, `web/src/lib/`, and `web/src/index.css` before adding UI. Reuse `Button`, `Modal`, `Select`, `RpeControl`, `Skeleton`, and shared motion components; extend existing patterns rather than making another button, dialog, card, or selector system.
- Feature actions use `Button`, including icon actions with explicit accessible names. Raw buttons and selects belong inside shared primitives; form controls and compact table rows must use `Select` rather than unstyled native popups to preserve Ayu dark/light styling, keyboard navigation, and custom scrollbars. Scrollbars app-wide and inside dropdowns/modals must use thin semantic Ayu colors (`var(--border-hover)` thumb, transparent track). Repeated field/error behavior should become one shared primitive using `web/src/lib/validation.ts`, not copied feature markup.
- Preserve Ayu light/dark themes and semantic variables in `web/src/index.css` (such as `--bg`, `--surface`, `--text`, `--muted`, `--accent`, `--on-accent`, `--red`, `--red-soft`, `--red-border`). Text on accent must use `var(--on-accent)`, not `var(--bg)`. Destructive/error tints must use `var(--red-soft)`/`var(--red-border)` rather than hard-coded rgba. `var(--overlay)` is reserved exclusively for backdrops and elevation box-shadows, never element backgrounds. Use bundled Inter Variable, tabular numbers, existing typography/spacing/radii, and visible focus. Do not introduce hard-coded feature colors, remote fonts, or sibling-app token names.
- Reuse `MotionScene`, `MotionPanel`, `SelectionIndicator`, and `useReducedMotion` from `ui/Motion.tsx`. Keep shared CSS durations/easing consistent with JS motion; motion must not delay content, focus, errors, or saves. Respect reduced motion and avoid decorative loops.
- Preserve compact/medium/expanded layout behavior at <640 / 640-1023 / >=1024 px. Verify 390/768/1440 px in both themes and the existing 320-1920 px responsive suite. Maintain 44 px compact/medium targets, no horizontal overflow, keyboard/focus behavior, accessible names, and dialog focus restoration.
- Keep action rows trailing-aligned or deliberately centered when they do not fill the row. Use factual labels, units, sources, uncertainty, and actionable errors. Passive status content is not a button; supporting diagnostics can use accessible disclosures.
- Use application-owned validation (`noValidate`, inline errors, `aria-invalid`/`aria-describedby`, first-invalid focus) backed by authoritative server checks. Preserve loading, empty, partial, offline, error, and busy states; update matching skeletons with layout changes.

## Code standardization

- Inspect Git status and preserve concurrent edits. Run commands in this repository, keep commits separate from sibling apps, and do not commit/push/deploy unless requested.
- Keep components and minimal API endpoints thin. Move reusable coordination into focused hooks, pure calculations into `web/src/lib/`, and backend rules into `api/Services/`. Reuse DTOs, validation, training/unit helpers, and request/error handling.
- Prefer explicit types, clear names, small functions, and readable multiline formatting. Avoid `any`, compressed logic, dead code, swallowed failures, and premature abstractions. Comments explain why.
- Aim for <=300 lines per new source file; split before 500 along responsibility boundaries. Do not grow oversized services: extract the responsibility being changed. Generated migrations are exempt. Never compress code to satisfy the limit.
- Server behavior is authoritative. Preserve tenancy, revision checks, transactions, idempotency, timeouts/cancellation, and structured error handling. Add regression coverage before changing authentication, save ordering, progression, or program lifecycle. Never rewrite applied migrations.

## Data boundaries

- FitnessAccount alone stores credentials. `IdentitySubject` is immutable; display name is mutable metadata. Consumer apps exchange OIDC codes server-side and use local sessions; never add consumer password storage or registration.
- The server remains authoritative for training data. Account-scoped IndexedDB may retain only the active workout recovery snapshot, unfinished set draft, and serializable pending edits/timing/finish operations needed to continue that session; all records stay partitioned by account, carry stable mutation identities, and reconcile through server revisions before replay. Do not cache training data in the service worker or retain completed history/program data locally. The device-local rest deadline is separately account/session scoped. Browser storage is best-effort, not a backup; static-shell caching must not cache API responses or return SPA HTML for API/asset failures.
- Kilograms are canonical and pounds are display conversion. Unknown load stays unknown and is excluded from volume; zero is a real bodyweight load. Freeze session bodyweight context so later weigh-ins do not rewrite historical calculations.
- Suggestions prefill but never pre-log sets or overwrite prescribed rep/RPE targets. Only completed sets enter history. Preserve effort, resistance-mode, phase-order, skip/completion, and draft-versus-finished semantics.
- The exercise catalog is global and seed-owned, read-only to users. Imports remain editable before program creation; preserve extracted/inferred/user-edited provenance and unresolved exercises instead of inventing matches.
- A PDF is read on the device: the browser extracts its text layer, plus the video links its annotation layer attaches to exercise names, and submits only those; no document bytes or page images reach the API or a model provider, and the links never enter a model request. A submitted link is re-validated server-side and kept only when it is an https video URL. The stored text is the whole source, is retained only while extraction is in flight, and is cleared at a terminal state. Pages without selectable text carry nothing and are reported, never guessed. What a model returns is reconciled, not refused: values are normalised into what a column holds and labelled inferred, and the outline's day count and duplicate-looking days are reported in review rather than failing a section. Sections are read concurrently and merged in outline order, committing an unbroken prefix so a failure leaves the rest retryable.
- Nutrition owns weight/goal context; Workout owns sessions/progression/PRs. Integration data may inform Workout suggestions, but must never automatically change Nutrition calorie/macronutrient targets. Keep tokens scoped and optional-provider failures bounded and visible where actionable.

## Verification

- Frontend: `web/`; API: `api/`; API regressions: `tests/`; browser tests: `web/tests/`. Use Node 24 and .NET 10.
- From repository root: `dotnet test tests/Workout.Tests.csproj`. From `web/`: `npm.cmd run check:docs`, `npm.cmd run check:standards`, `npm.cmd run typecheck`, `npm.cmd test`, `npm.cmd run build`; UI/shared-surface changes also require `npm.cmd run test:visual` and `npm.cmd run test:responsive`.
- Browser suites use a disposable API/database and local AI stand-in. Never run reset flows against production/personal data; serialize shared browser/build output during concurrent work.
- Verify documentation equality and `git diff --check`. Report checks run and explicitly label skipped tests, live providers, browser sign-in, and deployment checks as unverified.
